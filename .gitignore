# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
*.tsbuildinfo
.env
.env.*
__pycache__/
*.pyc

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Large generated/downloaded artifacts -- not source, and too large for plain git
# (no LFS configured). Rebuild via the data pipeline / tauri build rather than
# committing these; see HANDOVER.md for exact steps.
src-tauri/resources/bible.db
src-tauri/resources/commentaries.db
src-tauri/resources/model/
# Adams' Synchronological Chart: the source scan is a 218MB single JPEG, and the tile
# pyramid the Timeline panel actually loads is generated from it. Neither is source --
# regenerate the tiles with data-pipeline/build_chart_tiles.py; see HANDOVER.md.
public/Adams_Synchronological_Chart,_1881.jpg
public/chart/
data-pipeline/.venv/
data-pipeline/sources/
data-pipeline/output/
installer/
# machine-specific Cargo target-dir override (see config.toml.example)
src-tauri/.cargo/config.toml
